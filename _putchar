#include <unistd.h>

/**
 * my_unique_putchar - Writes a character to the standard output
 *
 * @c: The character to write
 * Return: On success 1, on error -1
 */
int my_unique_putchar(char c)
{
    return write(1, &c, 1);
}
